node_modules/
dist/
showcase/
.next/
*.tgz
storybook-static

.vercel
storybook-static/
.design-sync/sb-reference/
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules
.design-sync/*.log
.ds-sync/
ds-bundle/

# Generated by `vitest run --coverage`. Was accidentally committed in a
# `git add -A` and CodeQL flagged istanbul’s own sorter.js as a high-severity
# XSS — a true finding about vendored report output that should never have
# been in the tree.
coverage/

# Local design-sync tooling config, not part of the package.
.design-sync/
